FINAL PORTFOLIO ENHANCEMENT - COMPLETE IMPLEMENTATION

Implement ALL of the following enhancements in ONE response:

═══════════════════════════════════════════════════════════
PART 1: SKILLS WITH PROFICIENCY INDICATORS
═══════════════════════════════════════════════════════════

Update src/components/Skills.tsx with proficiency levels using dots + text labels:

Proficiency Data (EXACT levels):

ML Foundations:
- Supervised Learning: 4/5 (Advanced)
- Feature Engineering: 3/5 (Intermediate)
- Model Evaluation: 3/5 (Intermediate)
- Error Analysis: 4/5 (Advanced)
- Training Optimization: 3/5 (Intermediate)
- Performance Metrics: 3/5 (Intermediate)

Deep Learning:
- Neural Networks: 4/5 (Advanced)
- Transfer Learning: 3/5 (Intermediate)
- Model Architecture: 3/5 (Intermediate)
- Overfitting Prevention: 3/5 (Intermediate)

Data & Systems:
- Python: 4/5 (Advanced)
- Data Preprocessing: 4/5 (Advanced)
- Pandas / NumPy: 5/5 (Expert)
- SQL: 3/5 (Intermediate)
- API-based Inference: 2/5 (Beginner)
- Git: 3/5 (Intermediate)

Deployment:
- Flask: 2/5 (Beginner)
- REST APIs: 2/5 (Beginner)
- Latency Optimization: 3/5 (Intermediate)
- Model Serving: 3/5 (Intermediate)

Visual Format for Each Skill:
```tsx<div className="flex items-center justify-between mb-3">
  <span className="text-gray-200 dark:text-gray-200">Python</span>
  <div className="flex items-center gap-3">
    <div className="flex gap-1">
      {/* 4 filled dots, 1 empty */}
      <span className="w-2 h-2 rounded-full bg-teal-500"></span>
      <span className="w-2 h-2 rounded-full bg-teal-500"></span>
      <span className="w-2 h-2 rounded-full bg-teal-500"></span>
      <span className="w-2 h-2 rounded-full bg-teal-500"></span>
      <span className="w-2 h-2 rounded-full bg-gray-600"></span>
    </div>
    <span className="text-xs text-teal-400 font-medium">Advanced</span>
  </div>
</div>
````
Proficiency Label Logic:

1/5: Beginner
2/5: Beginner
3/5: Intermediate
4/5: Advanced
5/5: Expert

═══════════════════════════════════════════════════════════
PART 2: CONTACT FORM WITH FORMSPREE
═══════════════════════════════════════════════════════════
Update src/components/Contact.tsx:
Add ABOVE the existing contact cards:
{/* Contact Form */}
<form 
  action="https://formspree.io/f/YOUR_FORM_ID" 
  method="POST"
  className="max-w-2xl mx-auto mb-16"
>
  <div className="space-y-4">
    <input
      type="text"
      name="name"
      placeholder="Your Name"
      required
      className="w-full px-4 py-3 rounded-lg 
                 bg-gray-900/30 dark:bg-white/5 
                 border border-gray-700/50 dark:border-white/10
                 text-gray-900 dark:text-white 
                 placeholder-gray-500 dark:placeholder-gray-400
                 focus:border-teal-500 focus:outline-none focus:ring-2 focus:ring-teal-500/20
                 transition-all"
    />
    <input
      type="email"
      name="email"
      placeholder="Your Email"
      required
      className="w-full px-4 py-3 rounded-lg 
                 bg-gray-900/30 dark:bg-white/5 
                 border border-gray-700/50 dark:border-white/10
                 text-gray-900 dark:text-white 
                 placeholder-gray-500 dark:placeholder-gray-400
                 focus:border-teal-500 focus:outline-none focus:ring-2 focus:ring-teal-500/20
                 transition-all"
    />
    <textarea
      name="message"
      placeholder="Your Message"
      rows={5}
      required
      className="w-full px-4 py-3 rounded-lg 
                 bg-gray-900/30 dark:bg-white/5 
                 border border-gray-700/50 dark:border-white/10
                 text-gray-900 dark:text-white 
                 placeholder-gray-500 dark:placeholder-gray-400
                 resize-none
                 focus:border-teal-500 focus:outline-none focus:ring-2 focus:ring-teal-500/20
                 transition-all"
    />
    <button
      type="submit"
      className="w-full px-8 py-3 
                 bg-teal-500 hover:bg-teal-600 
                 text-white rounded-full font-medium 
                 transition-all duration-300
                 hover:shadow-lg hover:shadow-teal-500/20"
    >
      Send Message
    </button>
  </div>
</form><p className="text-center text-gray-500 dark:text-gray-400 mb-12">
  Or reach out directly:
</p>{/* Then existing contact cards */}

IMPORTANT: In the final code, replace `YOUR_FORM_ID` with a placeholder comment:
```tsxaction="https://formspree.io/f/YOUR_FORM_ID" // TODO: Sign up at formspree.io and replace with actual form ID

═══════════════════════════════════════════════════════════
PART 3: TYPING ANIMATION ON HERO
═══════════════════════════════════════════════════════════

Update src/components/Hero.tsx subtitle:

Replace the subtitle line with:
```tsx<h2 className="text-xl md:text-2xl text-teal-600 dark:text-teal-400 mb-4 font-light 
               typing-animation overflow-hidden whitespace-nowrap">
  Applied Machine Learning & Data Science
</h2>
````
Update src/index.css (add after existing animations):
@keyframes typing {
from { width: 0 }
to { width: 100% }
}@keyframes blink {
50% { border-color: transparent }
}.typing-animation {
width: 0;
animation: typing 3s steps(40) 1s forwards,
blink 0.75s step-end infinite;
border-right: 2px solid currentColor;
}/* Remove border after animation completes */
@media (prefers-reduced-motion: no-preference) {
.typing-animation {
animation: typing 3s steps(40) 1s forwards,
blink 0.75s step-end infinite 1s,
remove-border 0.01s 4s forwards;
}
}@keyframes remove-border {
to { border-right-color: transparent; }
}

═══════════════════════════════════════════════════════════
PART 4: FEATURED BADGE ON KALA
═══════════════════════════════════════════════════════════

Update src/components/Projects.tsx:

For the Kala project card ONLY, add badge ABOVE the title:
```tsx{/* Featured Badge - Only for Kala */}
{project.name === "Kala" && (
  <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full 
                  bg-amber-500/10 border border-amber-500/30 
                  text-amber-400 dark:text-amber-400
                  text-sm font-medium mb-4">
    <span>⭐</span>
    <span>Featured Project</span>
  </div>
)}
````
═══════════════════════════════════════════════════════════
PART 5: STATS SUMMARY BAR
═══════════════════════════════════════════════════════════
Create new component: src/components/StatsBar.tsx
import { motion } from 'framer-motion';export default function StatsBar() {
const stats = [
{ label: 'Projects', value: '3+' },
{ label: 'Skills', value: '15+' },
{ label: 'CGPA', value: '9.21' },
{ label: 'Hours Trained', value: '540+' }
];return (
<section className="py-12 border-y border-gray-900/10 dark:border-white/10">
<div className="max-w-6xl mx-auto px-6">
<div className="grid grid-cols-2 md:grid-cols-4 gap-8">
{stats.map((stat, index) => (
<motion.div
key={stat.label}
initial={{ opacity: 0, y: 20 }}
whileInView={{ opacity: 1, y: 0 }}
transition={{ delay: index * 0.1 }}
viewport={{ once: true }}
className="text-center"
>
<div className="text-3xl md:text-4xl font-bold text-teal-600 dark:text-teal-400 mb-2">
{stat.value}
</div>
<div className="text-sm text-gray-600 dark:text-gray-400 uppercase tracking-wide">
{stat.label}
</div>
</motion.div>
))}
</div>
</div>
</section>
);
}

Update src/App.tsx to include StatsBar AFTER Hero section:
```tsximport StatsBar from './components/StatsBar';// Inside App component:
<Hero />
<StatsBar />
<About />
{/* ... rest of sections */}

═══════════════════════════════════════════════════════════
PART 6: ENHANCED FOOTER
═══════════════════════════════════════════════════════════

Update src/components/Footer.tsx (or create if doesn't exist):
```tsximport { Github, Linkedin, Mail } from 'lucide-react';export default function Footer() {
const quickLinks = [
{ name: 'About', href: '#about' },
{ name: 'Skills', href: '#skills' },
{ name: 'Projects', href: '#projects' },
{ name: 'Contact', href: '#contact' }
];const socialLinks = [
{
name: 'GitHub',
href: 'https://github.com/b-rahul07',
icon: Github
},
{
name: 'LinkedIn',
href: 'https://linkedin.com/in/rahul-bandikolla',
icon: Linkedin
},
{
name: 'Email',
href: 'mailto:rahulbandikolla@gmail.com',
icon: Mail
}
];return (
<footer className="border-t border-gray-900/10 dark:border-white/10 
                    bg-gray-50/50 dark:bg-black/50 py-12">
<div className="max-w-6xl mx-auto px-6">
<div className="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
{/* Brand */}
<div>
<h3 className="text-2xl font-bold text-gray-900 dark:text-white mb-2">
B RAHUL.
</h3>
<p className="text-sm text-gray-600 dark:text-gray-400">
AI/ML Engineering Student
</p>
</div>      {/* Quick Links */}
      <div>
        <h4 className="font-semibold text-gray-900 dark:text-white mb-4">
          Quick Links
        </h4>
        <ul className="space-y-2">
          {quickLinks.map(link => (
            <li key={link.name}>
              <a 
                href={link.href}
                className="text-sm text-gray-600 dark:text-gray-400 
                           hover:text-teal-600 dark:hover:text-teal-400 
                           transition-colors"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>      {/* Connect */}
      <div>
        <h4 className="font-semibold text-gray-900 dark:text-white mb-4">
          Connect
        </h4>
        <div className="flex gap-4">
          {socialLinks.map(link => {
            const Icon = link.icon;
            return (                key={link.name}
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                className="w-10 h-10 rounded-full 
                           bg-gray-900/5 dark:bg-white/5 
                           border border-gray-900/10 dark:border-white/10
                           flex items-center justify-center
                           text-gray-600 dark:text-gray-400
                           hover:text-teal-600 dark:hover:text-teal-400
                           hover:border-teal-500/50
                           transition-all"
                aria-label={link.name}
              >
                <Icon className="w-5 h-5" />
              </a>
            );
          })}
        </div>
      </div>
    </div>    {/* Bottom Bar */}
    <div className="pt-8 border-t border-gray-900/10 dark:border-white/10 
                    flex flex-col md:flex-row justify-between items-center gap-4">
      <p className="text-sm text-gray-600 dark:text-gray-400">
        © 2026 Rahul Bandikolla. All rights reserved.
      </p>
      <p className="text-xs text-gray-500 dark:text-gray-500">
        Built with React + TypeScript + Vite
      </p>
    </div>
  </div>
</footer>
);
}

═══════════════════════════════════════════════════════════
PART 7: SUBTLE PARTICLE BACKGROUND ON HERO
═══════════════════════════════════════════════════════════

Create new component: src/components/ParticleBackground.tsx
```tsximport { useEffect, useRef } from 'react';export default function ParticleBackground() {
const canvasRef = useRef<HTMLCanvasElement>(null);useEffect(() => {
const canvas = canvasRef.current;
if (!canvas) return;const ctx = canvas.getContext('2d');
if (!ctx) return;// Set canvas size
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;// Particle configuration
const particles: Array<{
  x: number;
  y: number;
  vx: number;
  vy: number;
  radius: number;
}> = [];const particleCount = 50;
const colors = ['rgba(20, 184, 166, 0.3)', 'rgba(6, 182, 212, 0.3)'];// Create particles
for (let i = 0; i < particleCount; i++) {
  particles.push({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height,
    vx: (Math.random() - 0.5) * 0.5,
    vy: (Math.random() - 0.5) * 0.5,
    radius: Math.random() * 2 + 1
  });
}// Animation loop
function animate() {
  if (!ctx || !canvas) return;  ctx.clearRect(0, 0, canvas.width, canvas.height);  particles.forEach((particle, index) => {
    // Update position
    particle.x += particle.vx;
    particle.y += particle.vy;    // Wrap around edges
    if (particle.x < 0) particle.x = canvas.width;
    if (particle.x > canvas.width) particle.x = 0;
    if (particle.y < 0) particle.y = canvas.height;
    if (particle.y > canvas.height) particle.y = 0;    // Draw particle
    ctx.beginPath();
    ctx.arc(particle.x, particle.y, particle.radius, 0, Math.PI * 2);
    ctx.fillStyle = colors[index % colors.length];
    ctx.fill();
  });  requestAnimationFrame(animate);
}animate();// Handle resize
const handleResize = () => {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
};window.addEventListener('resize', handleResize);
return () => window.removeEventListener('resize', handleResize);
}, []);return (
<canvas
ref={canvasRef}
className="absolute inset-0 pointer-events-none"
style={{ opacity: 0.4 }}
/>
);
}

Update src/components/Hero.tsx:
```tsximport ParticleBackground from './ParticleBackground';// Inside Hero component, at the very top of the return statement:
<section className="relative min-h-screen ...">
  <ParticleBackground />{/* Rest of hero content */}
</section>
````
═══════════════════════════════════════════════════════════
PART 8: CERTIFICATION LOGOS
═══════════════════════════════════════════════════════════
Update src/components/Certifications.tsx:
Add logo images for each certification:
const certifications = [
{
title: "Salesforce Certified Agentforce Specialist",
issuer: "Salesforce",
logo: "https://upload.wikimedia.org/wikipedia/commons/f/f9/Salesforce.com_logo.svg",
date: "December 16, 2025",
id: "7198343",
verifyUrl: "#"
},
{
title: "Artificial Intelligence Application Developer",
issuer: "NIELIT Chennai (Govt. of India)",
logo: "https://rect.nielitchennai.edu.in/images/nielitlogo.png",
date: "November 25, 2025",
id: "1796690",
details: "NCrF Level 4.5, 540 Hours Duration",
verifyUrl: "#"
},
{
title: "Deep Learning (Elite Certification)",
issuer: "NPTEL (IIT Ropar)",
logo: "https://nptel.ac.in/img/nptel-logo.png",
date: "Jul–Oct 2025",
id: "NPTEL25CS106S458204855",
details: "75% Score (Elite Status)",
verifyUrl: "#"
}
];// In the card rendering:
<div className="flex items-start gap-4 mb-4">
  <img 
    src={cert.logo} 
    alt={`${cert.issuer} logo`}
    className="w-12 h-12 object-contain"
    onError={(e) => {
      // Fallback to text if image fails
      e.currentTarget.style.display = 'none';
    }}
  />
  <div className="flex-1">
    <h3 className="text-lg font-semibold text-gray-900 dark:text-white">
      {cert.title}
    </h3>
    <p className="text-teal-600 dark:text-teal-400 text-sm">
      {cert.issuer}
    </p>
  </div>
</div>
```
═══════════════════════════════════════════════════════════
CRITICAL REQUIREMENTS:
═══════════════════════════════════════════════════════════

✅ Provide COMPLETE file contents for ALL modified files
✅ Maintain existing functionality - don't break anything
✅ Use theme-aware classes (dark: prefix for all colors)
✅ All animations should be smooth and performant
✅ Responsive design (mobile-first, test sm:, md:, lg:)
✅ TypeScript types must be correct
✅ Import all necessary dependencies

═══════════════════════════════════════════════════════════
DELIVERABLES - COMPLETE ALL NOW:
═══════════════════════════════════════════════════════════
Provide complete updated code for:

✓ src/components/Skills.tsx (with proficiency dots + labels)
✓ src/components/Contact.tsx (with Formspree form)
✓ src/components/Hero.tsx (with typing animation)
✓ src/components/Projects.tsx (with Featured badge on Kala)
✓ src/components/StatsBar.tsx (NEW file)
✓ src/components/Footer.tsx (enhanced version)
✓ src/components/ParticleBackground.tsx (NEW file)
✓ src/components/Certifications.tsx (with logos)
✓ src/index.css (with typing animation keyframes)
✓ src/App.tsx (with StatsBar added after Hero)

START NOW. This is the final enhancement - make it perfect! 🚀
