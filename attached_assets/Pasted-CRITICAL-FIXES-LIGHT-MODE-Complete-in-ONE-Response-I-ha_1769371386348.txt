CRITICAL FIXES + LIGHT MODE - Complete in ONE Response

I have 30% credits left. Fix these issues and add light mode:

═══════════════════════════════════════════════════════════
PRIORITY 1: FIX ABOUT SECTION (src/components/About.tsx)
═══════════════════════════════════════════════════════════

CURRENT ISSUES:
❌ Black & white profile photo (bad)
❌ Photo placement is awkward
❌ Weird hover effect on photo

NEW DESIGN - Clean & Professional:

REMOVE the profile photo entirely from About section.
(We removed it from Hero - keep consistency)

LAYOUT - Text-Focused:
┌────────────────────────────────────────────────────────┐
│                                                        │
│  01 About Me                                           │
│  (Large "01" in teal/10 opacity, "About Me" white)     │
│                                                        │
│  Bridging the gap between conceptual AI and            │
│  deployable engineering.                               │
│  (Subtitle in teal-400, text-xl)                       │
│                                                        │
│  I focus on moving models from notebooks to            │
│  production. My approach centers on rigorous dataset   │
│  design, architectural selection, and measurable       │
│  performance evaluation.                               │
│  (Paragraph 1, text-gray-300)                          │
│                                                        │
│  I build end-to-end pipelines—handling everything      │
│  from data preprocessing and feature engineering to    │
│  inference API deployment.                             │
│  (Paragraph 2, text-gray-300)                          │
│                                                        │
│  [3 Capability Cards in Row - Full Width]              │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐      │
│  │ 🧠         │  │ 🚀         │  │ ⚡         │      │
│  │ Deep       │  │ ML         │  │ Model      │      │
│  │ Learning   │  │ Deployment │  │ Evaluation │      │
│  │            │  │            │  │            │      │
│  │ Neural     │  │ End-to-end │  │ Performance│      │
│  │ networks,  │  │ pipelines  │  │ metrics,   │      │
│  │ transformer│  │ from train │  │ error      │      │
│  │ architect. │  │ to prod    │  │ analysis   │      │
│  └────────────┘  └────────────┘  └────────────┘      │
└────────────────────────────────────────────────────────┘

STYLING:
- Remove ALL profile photos/images
- Section number "01": text-8xl font-bold text-gray-800 (visible but subtle)
- Subtitle: text-xl md:text-2xl text-teal-400
- Paragraphs: text-base md:text-lg text-gray-300 leading-relaxed
- Max width: max-w-4xl mx-auto
- Capability cards:
```tsx
  className="backdrop-blur-sm bg-white/5 border border-white/10 
             rounded-xl p-6 hover:border-teal-500/30 transition-all"
```

PROVIDE COMPLETE About.tsx with NO profile photo.

═══════════════════════════════════════════════════════════
PRIORITY 2: LIGHT/DARK MODE TOGGLE
═══════════════════════════════════════════════════════════

Implement complete theme system:

1. CREATE src/context/ThemeContext.tsx:
```typescript
import { createContext, useContext, useEffect, useState } from 'react';

type Theme = 'dark' | 'light';

interface ThemeContextType {
  theme: Theme;
  toggleTheme: () => void;
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>(() => {
    // Check localStorage first, then system preference
    const saved = localStorage.getItem('theme') as Theme;
    if (saved) return saved;
    
    return window.matchMedia('(prefers-color-scheme: dark)').matches 
      ? 'dark' 
      : 'light';
  });

  useEffect(() => {
    // Apply theme to document
    document.documentElement.classList.remove('light', 'dark');
    document.documentElement.classList.add(theme);
    localStorage.setItem('theme', theme);
  }, [theme]);

  const toggleTheme = () => {
    setTheme(prev => prev === 'dark' ? 'light' : 'dark');
  };

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) throw new Error('useTheme must be used within ThemeProvider');
  return context;
}
```

2. UPDATE src/main.tsx:
```typescript
import { ThemeProvider } from './context/ThemeContext';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ThemeProvider>
      <App />
    </ThemeProvider>
  </React.StrictMode>
);
```

3. UPDATE src/components/Navbar.tsx:
Add theme toggle button:
```tsx
import { Moon, Sun } from 'lucide-react';
import { useTheme } from '../context/ThemeContext';

// Inside Navbar component:
const { theme, toggleTheme } = useTheme();

// Add button next to Resume:
<button
  onClick={toggleTheme}
  className="p-2 rounded-full bg-white/5 border border-white/10 
             hover:border-teal-500/50 transition-all"
  aria-label="Toggle theme"
>
  {theme === 'dark' ? (
    <Sun className="w-5 h-5 text-teal-400" />
  ) : (
    <Moon className="w-5 h-5 text-teal-600" />
  )}
</button>
```

4. UPDATE tailwind.config.js:
```javascript
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class', // Enable class-based dark mode
  theme: {
    extend: {
      // Your existing config...
    },
  },
  plugins: [],
};
```

5. UPDATE src/index.css:
Add light mode color variables:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  /* Dark mode (default) */
  .dark {
    --bg-primary: #0a0a0a;
    --bg-secondary: #1a1a1a;
    --text-primary: #ffffff;
    --text-secondary: #d1d5db;
    --accent: #14b8a6;
    --border: rgba(255, 255, 255, 0.1);
  }

  /* Light mode */
  .light {
    --bg-primary: #ffffff;
    --bg-secondary: #f9fafb;
    --text-primary: #0a0a0a;
    --text-secondary: #4b5563;
    --accent: #0d9488;
    --border: rgba(0, 0, 0, 0.1);
  }

  body {
    background-color: var(--bg-primary);
    color: var(--text-primary);
  }
}

/* Smooth theme transition */
* {
  transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
}

/* Keep your existing animations */
@keyframes gradient {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
}

html { scroll-behavior: smooth; }
```

6. UPDATE COLOR CLASSES ACROSS ALL COMPONENTS:

Replace hardcoded colors with theme-aware classes:

DARK MODE → LIGHT MODE MAPPINGS:
- `bg-[#0a0a0a]` → `bg-white dark:bg-[#0a0a0a]`
- `text-white` → `text-gray-900 dark:text-white`
- `text-gray-300` → `text-gray-600 dark:text-gray-300`
- `text-gray-400` → `text-gray-500 dark:text-gray-400`
- `bg-white/5` → `bg-gray-900/5 dark:bg-white/5`
- `border-white/10` → `border-gray-900/10 dark:border-white/10`
- `text-teal-400` → `text-teal-600 dark:text-teal-400`
- `bg-teal-500` → `bg-teal-500 dark:bg-teal-500` (keep same)
- `border-teal-500/20` → `border-teal-500/30 dark:border-teal-500/20`

UPDATE THESE COMPONENTS with theme-aware classes:
- src/components/Hero.tsx
- src/components/About.tsx
- src/components/Skills.tsx
- src/components/Projects.tsx
- src/components/Navbar.tsx
- src/components/Experience.tsx
- src/components/Education.tsx
- src/components/Certifications.tsx
- src/components/Contact.tsx

GLASSMORPHISM IN LIGHT MODE:
```tsx
// Dark mode
className="backdrop-blur-md bg-white/5 border border-white/10"

// Light mode aware
className="backdrop-blur-md bg-gray-900/5 dark:bg-white/5 
           border border-gray-900/10 dark:border-white/10"
```

═══════════════════════════════════════════════════════════
DELIVERABLES - COMPLETE ALL NOW:
═══════════════════════════════════════════════════════════

1. ✓ src/components/About.tsx (fixed, no photo)
2. ✓ src/context/ThemeContext.tsx (new file)
3. ✓ src/main.tsx (wrapped with ThemeProvider)
4. ✓ src/components/Navbar.tsx (with theme toggle button)
5. ✓ tailwind.config.js (darkMode: 'class')
6. ✓ src/index.css (light mode CSS variables)
7. ✓ ALL component updates with dark: prefix classes:
   - Hero.tsx
   - About.tsx
   - Skills.tsx
   - Projects.tsx
   - Experience.tsx
   - Education.tsx
   - Certifications.tsx
   - Contact.tsx

PROVIDE COMPLETE FILES FOR ALL OF THE ABOVE.

Focus on:
- Clean About section (no photo)
- Working light/dark toggle
- All components support both themes
- Smooth transitions
- Preserve all visual effects in both modes